# Preliminary handler layout.

window/:
screen  putty  macos  gnome

transfer/:
samba  rsync  putty  openssh  mscifs

execute/:
winexe  putty  psexec  openssh

auth/:
samba  putty  openssh  mscifs

access/:
rdesktop  putty  openssh  mstsc

host/:
vm  os  oob  lom

host/vm/:
xen  vmw  kvm  hpvm

host/os/:
solaris  rhel  openbsd  netbsd  hpux  freebsd  debian  aix

host/oob/:
wol

host/lom/:
ilo

