
case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac

case "$meh" in
    foo)
    echo "foo: $foo"
    bar)
    echo "bar: $bar"
    *)
    echo "eek!"
esac
